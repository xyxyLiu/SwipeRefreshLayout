<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.reginald.swiperefresh.sample">

    <uses-sdk android:minSdkVersion="8" android:targetSdkVersion="17" />

    <application
        android:noHistory="true"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/AppTheme">


        <activity android:name=".MainActivity"
            android:label= "@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListViewDemoActivity"
            android:label= "A ListView demo"
            android:screenOrientation="portrait">
        </activity>
        <activity android:name=".ScrollViewDemoActivity"
            android:label= "A ScrollView in a LinearLayout demo"
            android:screenOrientation="portrait">
        </activity>
        <activity android:name=".ViewPagerDemoActivity"
            android:label= "A ViewPager in LinearLayout demo"
            android:screenOrientation="portrait">
        </activity>

    </application>

</manifest>
